# Server Configuration
# Copy this file to 'env' and update the values as needed

# Server port (default: 3000)
PORT=3000

# Domain for CORS configuration (e.g., "example.com", "localhost", "127.0.0.1")
# This is used to build CORS allowed origins
DOMAIN=example.com

# Full CORS origin URL (e.g., "http://example.com:5173", "http://localhost:5173")
# This is the primary CORS origin for your frontend
CORS_ORIGIN=http://example.com:5173

# Example configurations:
# For local development:
# DOMAIN=localhost
# CORS_ORIGIN=http://localhost:5173

# For production with domain "myapp.com":
# DOMAIN=myapp.com
# CORS_ORIGIN=https://myapp.com

# For IP address:
# DOMAIN=127.0.0.1
# CORS_ORIGIN=http://127.0.0.1:5173 